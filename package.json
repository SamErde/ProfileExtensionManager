{
  "name": "personas",
  "displayName": "Personas",
  "description": "See and manage which extensions are installed in each VS Code profile.",
  "version": "0.8.4",
  "publisher": "SamErde",
  "license": "MIT",
  "engines": {
    "vscode": "^1.90.0"
  },
  "categories": [
    "Other"
  ],
  "extensionKind": [
    "ui"
  ],
  "icon": "media/icon-256.png",
  "galleryBanner": {
    "color": "#16213E",
    "theme": "dark"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/SamErde/ProfileExtensionManager"
  },
  "main": "./dist/extension.js",
  "activationEvents": [],
  "contributes": {
    "commands": [
      {
        "command": "profileExtensionManager.showMatrix",
        "title": "Show Extension Matrix",
        "category": "Profile Extension Manager",
        "icon": "$(extensions)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "profileExtensionManager",
          "title": "Profile Extension Manager",
          "icon": "media/activity-icon.svg"
        }
      ]
    },
    "views": {
      "profileExtensionManager": [
        {
          "id": "profileExtensionManager.welcome",
          "name": "Profiles",
          "type": "webview"
        }
      ]
    },
    "configuration": {
      "title": "Profile Extension Manager",
      "properties": {
        "profileExtensionManager.openMatrixOnActivityBarClick": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "When enabled, activating the Profile Extension Manager sidebar automatically opens the Extension Matrix — including on window restore if the sidebar was left active. Disable to require clicking the \"Open Extension Matrix\" button instead."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "vscode:prepublish": "npm run build",
    "watch": "node esbuild.mjs --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:integration": "npm run build && tsc -p tsconfig.integration.json && node out-test/test/integration/runTests.js",
    "package": "node -e \"require('fs').mkdirSync('releases',{recursive:true})\" && vsce package --out releases/"
  },
  "devDependencies": {
    "@types/node": "^22",
    "@types/vscode": "^1.90.0",
    "@vscode/test-electron": "^3.0.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  }
}
